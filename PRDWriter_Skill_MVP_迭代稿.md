# PRDWriter Skill MVP 迭代稿

> 目标：让 AI Agent 先和用户聊清楚需求，再产出可用于产品、设计、研发、测试或 vibe coding 的标准 PRD。

## 1. MVP 范围

本版先做一件事：把“模糊想法”变成“结构化 PRD”。

暂不做网页可视化流程、在线表单、自动画原型、自动生成代码。等这个 MVP 跑通后，再做可视化页面。

## 2. 触发条件

满足任一条件时使用：

- 用户描述产品想法，例如“我想做一个……”
- 用户提出功能需求，例如“能不能加一个……”
- 用户提出改进意见，例如“这个页面想优化一下……”
- 用户明确要求写 PRD、需求文档、MVP 文档、产品方案、功能说明。
- 用户要求写漫剧项目 PRD、短视频连载项目 PRD、漫画短剧方案或剧情项目需求。

## 3. 核心约束

在完成“概念版对齐”之前，不输出完整 PRD、完整功能列表或过深的结构化文档。

原因很简单：还没确认方向就写 PRD，容易把错误方向写得很完整，后面返工更大。

新增约束：

- 聊需求过程中要同步生成并更新一份 PRD Markdown。
- 文件按项目命名，默认放在 `./<项目名>/<项目名>_PRD.md`。
- 完成 PRD 后，生成同名网页 `./<项目名>/<项目名>_PRD.html`。
- 网页使用“皮玺玉风格”：白底、清爽、产品经理笔记感、少量青绿/蓝/浅紫/玫红高亮，适合评审和交付。

## 4. 四阶段流程

### Phase 1：三视角诊断

用用户、商业、技术三个视角快速判断需求是否站得住。

用户视角：

- 目标用户是谁？
- 用户在什么场景遇到什么问题？
- 现在怎么解决？哪里不爽？
- 这个问题发生频率高不高、痛感强不强？

商业视角：

- 免费还是付费？
- 如果收费，是一次性买断、订阅、会员增值还是抽佣？
- 这是验证型项目还是长期运营项目？
- 对收入、留存、效率、成本、品牌或战略有什么价值？

技术视角：

- 产品形态是什么：小程序、iOS、Android、H5、Web、Web App、插件？
- 是否需要账号、登录、角色、权限？
- 是否依赖已有系统、第三方服务、AI、支付、地图、文件、风控或合规？
- 是否有数据、埋点、性能、安全、兼容性要求？

成功标志：能得到一个简短诊断表，并列出关键待确认问题。

同步产物：

- 创建或更新 `<项目名>_PRD.md`。
- 写入“需求诊断”部分，包括三视角诊断、假设和待确认问题。
- 文档状态标记为“诊断中”。

### Phase 2：概念版对齐

输出一个很短的概念版，让用户确认方向。

概念版必须包含：

- 一句话产品定义
- 目标用户
- 核心场景
- 核心功能（MVP 不超过 3 个）
- 平台选择
- 本版不做
- 成功指标
- 关键风险和待确认项

成功标志：用户明确回复“确认”或给出修改意见。

同步产物：

- 更新 `<项目名>_PRD.md` 的“概念版对齐”部分。
- 文档状态标记为“概念待确认”。

### Phase 3：落地信息补齐

确认概念后，再补 PRD 细节：

- 主流程、分支流程、异常流程
- 页面流、入口、退出、返回、跳转
- 权限、账号角色、数据展示、表单、控件、弹窗、文案
- 空状态、加载、离线、失败、撤销、二次确认
- 数据字段、埋点、指标、验收标准
- 性能、安全、稳定性、合规、运营、排期、依赖和风险

成功标志：关键需求已经能写成可执行规则，而不是停留在“支持/优化/提升”。

同步产物：

- 每补齐一批已确认信息，就更新 `<项目名>_PRD.md`。
- 对缺失信息使用“待确认”，不留空。

### Phase 4：标准 PRD 输出

最终输出 Markdown PRD，建议结构：

1. 文档说明：版本、修订记录、术语表
2. 背景与目标：背景、目标用户、业务目标、成功指标、价值分析
3. 范围定义：一句话定义、MVP 范围、本版不做、假设与约束
4. 产品方案：功能结构、信息结构、业务流程、页面流程
5. 全局规则：权限、角色、状态、文案、交互
6. 需求列表：编号、模块、需求、类型、优先级、迭代、说明
7. 需求详情：用户故事、业务规则、交互、字段、验收标准、异常
8. 非功能需求：性能、稳定性、安全、合规、兼容性
9. 埋点与数据：事件、参数、用途
10. 项目计划：阶段、交付物、负责人、状态、风险
11. 风险与待确认：问题、影响、建议、状态

成功标志：研发能照着拆任务，设计能照着画界面，测试能照着写用例。

同步产物：

- `<项目名>_PRD.md` 成为最终标准 PRD。
- 生成同名网页 `<项目名>_PRD.html`。
- Markdown 与 HTML 的主要章节必须一致。

## 5. PRD 质量检查表

需求真实性：

- 是否符合核心业务场景？
- 是否符合用户画像和用户故事？
- 是否有竞品、用户反馈或数据支撑？
- 是共性需求，还是单个用户的一次性请求？

价值：

- 对核心用户的影响能否量化？
- 对核心业务的贡献能否量化？
- 是否影响收入、留存、效率、成本、KPI 或满意度？

可行性：

- 当前技术是否支持？
- 当前业务和运营是否支持？
- 资源、成本、依赖、排期是否明确？

风险：

- 是否影响已有功能？
- 是否有安全、风控、法律、合规、舆情风险？
- 上线后如何验证效果？

交互细节：

- 是否覆盖空状态、加载、失败、离线、无权限？
- 是否需要二次确认、撤销、取消、重试？
- 表单是否定义校验、默认值、提示、长度、特殊字符、敏感词？
- 数据展示是否定义分页、排序、筛选、缓存、实时性、过期提示？

## 6. 下一步迭代方向

MVP 测通后再做：

- 网页可视化：已纳入本技能输出，先生成静态 PRD 网页；后续再做可点选工作台。
- PRD 生成器：表单式收集信息，一键导出 Markdown。
- 原型联动：根据确认后的 MVP 功能生成低保真或 HTML 原型。
- 测试用例联动：从验收标准自动生成测试点。
- 版本对比：自动维护修订记录和变更差异。

## 7. MVP 测试用例

### 测试 1：用户只有模糊想法

输入：

> 我想做一个给健身房教练用的学员打卡小程序，帮我写 PRD。

期望输出：

- 不能直接输出完整 PRD。
- 先进入 Phase 1，按用户、商业、技术三类提问。
- 如果有合理猜测，必须标注为待确认。

成功标志：

- 输出里有三视角诊断或问题清单。
- 没有完整需求列表、完整功能详情和开发排期。

### 测试 2：用户确认概念版

输入：

> 目标用户是私教，平台是微信小程序，先做学员打卡、训练计划、进度统计，收费先不做。

期望输出：

- 进入 Phase 2，输出概念版。
- 核心功能不超过 3 个。
- 明确“本版不做”，例如支付、会员、复杂课程商城。
- 要求用户确认后再进入 PRD 详情。

成功标志：

- 概念版包含一句话定义、目标用户、核心场景、核心功能、平台、本版不做、成功指标、风险。

### 测试 3：用户要求立即产出 PRD

输入：

> 就按你理解的写完整 PRD。

期望输出：

- 如果概念版已确认，可以进入 Phase 4。
- 如果仍缺关键条件，允许产出草稿，但必须把高风险信息标为待确认。

成功标志：

- PRD 使用标准 Markdown 模板。
- 每个核心功能都有用户故事、规则、验收标准、异常情况。
- 有风险与待确认表。

### 测试 4：生成同步 Markdown 和同名网页

输入：

> 项目名叫私教打卡助手，概念版确认，按标准 PRD 输出。

期望输出：

- 生成 `私教打卡助手/私教打卡助手_PRD.md`。
- 完成后生成 `私教打卡助手/私教打卡助手_PRD.html`。
- 网页是白底清爽的皮玺玉风格，项目名和文档状态在首屏可见。

成功标志：

- Markdown 与 HTML 主要章节一致。
- HTML 可直接浏览器打开，不需要启动服务。
- 移动端窄屏下表格不挤压文字，允许横向滚动。

### 测试 5：漫剧项目 PRD

输入：

> 帮我写一份漫剧项目 PRD。
> 题材：都市复仇爽文
> 集数：30集
> 每集：60秒
> 风格：韩漫感，强情绪，快节奏
> 目标：适合短视频平台连载
> 请包含人物设定、剧情结构、分集节奏、画面风格和验收标准。

期望输出：

- 识别为“漫剧项目”，不按普通软件功能 PRD 写。
- PRD 用来定义这部漫剧“要做成什么样”。
- 包含题材、目标观众、主角人设、爽点类型、集数、每集时长、画风、输出格式、禁止内容、成功标准。
- 包含人物设定、剧情结构、分集节奏、画面风格和验收标准。

成功标志：

- 一眼能看懂这部剧到底要做什么。
- 编剧、分镜、画师、剪辑或投放团队能照着统一方向继续拆解。
