interface:
  display_name: "PRD Writer Agent"
  short_description: "通过需求访谈同步产出PRD Markdown和同名评审网页"
  default_prompt: "Use $prd-writer-agent to clarify my product idea, maintain a PRD Markdown file, and generate a matching PRD web page."
